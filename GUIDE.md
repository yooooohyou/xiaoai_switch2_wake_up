# 小爱音箱 BLE 唤醒器 — 从烧录到使用

ESP32-C3 通过米家 Matter 协议接收开关指令，触发 BLE 广播唤醒小爱音箱。

---

## 目录

1. [硬件准备](#1-硬件准备)
2. [开发环境安装](#2-开发环境安装)
3. [获取代码](#3-获取代码)
4. [编译 & 烧录](#4-编译--烧录)
5. [首次配网（接入米家）](#5-首次配网接入米家)
6. [日常使用](#6-日常使用)
7. [按键说明](#7-按键说明)
8. [LED 状态说明](#8-led-状态说明)
9. [自定义配置](#9-自定义配置)
10. [常见问题](#10-常见问题)

---

## 1. 硬件准备

| 器件 | 说明 |
|---|---|
| ESP32-C3 开发板 | 合宙 AIR32C3 或兼容板均可 |
| USB-C 数据线 | 用于烧录，需支持数据传输 |
| 5V 电源 | 烧录完成后日常供电用 |

**引脚定义（代码默认值）：**

| 功能 | GPIO |
|---|---|
| 触发按键 | GPIO 9（低电平触发，内部上拉）|
| 状态 LED | GPIO 12 |
| 开关状态 LED | GPIO 13 |

---

## 2. 开发环境安装

### 安装 VS Code + PlatformIO

1. 下载安装 [VS Code](https://code.visualstudio.com/)
2. 打开 VS Code → 扩展（Ctrl+Shift+X）→ 搜索 `PlatformIO IDE` → 安装
3. 等待 PlatformIO 完成初始化（约 2-5 分钟）

> **注意：** 本项目使用 arduino-esp32 3.x（内置 Matter SDK），首次编译时 PlatformIO 会自动下载约 500MB 的工具链，请保持网络畅通。

---

## 3. 获取代码

```bash
git clone https://github.com/yooooohyou/xiaoai_switch2_wake_up.git
cd xiaoai_switch2_wake_up
git checkout claude/chinese-responses-443Vv
```

用 VS Code 打开该目录：

```bash
code .
```

---

## 4. 编译 & 烧录

### 4.1 连接设备

将 ESP32-C3 通过 USB-C 连接电脑。

- **合宙 AIR32C3**：直接插上即可识别，无需额外驱动
- 若设备未识别，安装 [CH343 驱动](https://www.wch.cn/downloads/CH343SER_ZIP.html)

### 4.2 编译

点击 VS Code 底部工具栏的 **✓ Build**，或按快捷键：

```
Ctrl+Alt+B
```

首次编译需要下载依赖，Matter SDK 较大，**预计耗时 5-15 分钟**，后续编译约 1-2 分钟。

编译成功的标志：

```
========================= [SUCCESS] Took XX.XX seconds =========================
```

### 4.3 烧录

点击底部工具栏的 **→ Upload**，或按快捷键：

```
Ctrl+Alt+U
```

烧录成功后设备自动重启，LED 开始快速闪烁。

> **烧录失败？** 部分 ESP32-C3 需要手动进入 Boot 模式：按住 BOOT 键不放 → 按一下 RST 键 → 松开 BOOT 键，再重试烧录。

---

## 5. 首次配网（接入米家）

烧录完成后，按以下步骤将设备接入米家：

### 步骤一：连接引导 WiFi

设备上电后 LED 快速闪烁，同时自动开启一个临时 WiFi：

| 项目 | 内容 |
|---|---|
| WiFi 名称 | `ESP32_Matter_Setup` |
| WiFi 密码 | `12345678` |

用手机连接该 WiFi。

### 步骤二：获取配对码

连接后打开手机浏览器，访问：

```
http://192.168.4.1
```

页面会显示：
- **11 位手动配对码**（红色大字）
- **"点击生成二维码"** 按钮 → 跳转到二维码生成页，截图保存

### 步骤三：米家 App 添加设备

1. **断开** `ESP32_Matter_Setup` WiFi，重新连接家庭 WiFi
2. 打开**米家 App** → 右上角 **+** → **添加设备**
3. 选择 **扫描二维码** 或 **手动输入配对码**
4. 按 App 提示操作，输入家庭 WiFi 密码
5. 等待配对完成（约 30-60 秒）

### 步骤四：配对成功

配对成功后：
- 引导 WiFi 自动关闭
- LED 由快闪变为**慢闪**（每 2 秒闪一次）
- 米家 App 中出现 **"开关"** 设备

---

## 6. 日常使用

配对完成后，在米家 App 中操作该设备：

| 米家指令 | 设备行为 |
|---|---|
| **开** | 发送 BLE 广播（持续约 1 秒），唤醒目标小爱音箱 |
| **关** | 停止 BLE 广播 |

也可以通过**小爱同学**语音控制：

```
"小爱同学，打开 [设备名称]"
```

或将其加入**米家自动化**：例如每天早上 7 点自动唤醒音箱。

---

## 7. 按键说明

设备上的按键（GPIO 9）：

| 操作 | 未配对时 | 已配对时 |
|---|---|---|
| **短按**（< 3 秒）| 重启引导 WiFi（忘记配对码时用）| 手动触发一次 BLE 唤醒广播（测试用）|
| **长按**（> 3 秒）| 出厂重置 | 出厂重置（Matter 解配对 + 清除所有配置）|

> **出厂重置**后设备会重启，LED 重新快闪，需重新走配网流程。

---

## 8. LED 状态说明

| LED 状态 | 含义 |
|---|---|
| 快速闪烁（每 0.3 秒）| 未配对，等待米家扫码 |
| 慢速闪烁（每 2 秒）| 已配对，正常运行 |
| GPIO 13 常亮 | 米家 App 当前状态为"开" |
| GPIO 13 熄灭 | 米家 App 当前状态为"关" |

---

## 9. 自定义配置

### 修改 BLE 唤醒目标

如需唤醒不同的小爱音箱，修改 `src/main.cpp` 中的两处：

```cpp
// 广播数据（抓包获取，对应目标音箱的 MAC）
static uint8_t wake_adv_data[] = {
    0x02, 0x01, 0x06,
    0x1B, 0xFF,
    // ... 替换为目标设备的广播数据
};

// 基础 MAC（对应目标音箱绑定手机的 WiFi MAC）
uint8_t baseMAC[6] = {0x78, 0x81, 0x8c, 0x06, 0x9a, 0xc4};
```

### 修改 Matter 配对码

默认配对码为测试用途，生产使用建议修改：

```cpp
// src/main.cpp
#define MATTER_PASSCODE      20202021   // 替换为 8 位数字（不能全相同）
#define MATTER_DISCRIMINATOR 3840       // 0-4095 任意值
```

修改后重新编译烧录，引导 WiFi 页面会显示新的配对码。

### 修改引导 WiFi 密码

```cpp
// src/main.cpp
#define SETUP_AP_PASSWORD "12345678"   // 替换为新密码
```

---

## 10. 常见问题

**Q：编译报错 "Matter.h: No such file"**

平台版本不对。检查 `platformio.ini` 中的 `platform` 字段：

```ini
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.10/platform-espressif32.zip
```

确保完整，然后执行 PlatformIO → Clean，再重新编译。

---

**Q：烧录成功但 LED 不亮**

检查引脚定义是否与实际硬件对应（`LED_PIN`、`TRIGGER_PIN`），不同版本开发板引脚可能不同。

---

**Q：192.168.4.1 打不开**

- 确认手机已连接 `ESP32_Matter_Setup`，而不是家庭 WiFi
- 部分手机连接无互联网 WiFi 时会自动切换回家庭 WiFi，需在系统设置中手动保持连接
- 也可短按设备按键重启引导 WiFi

---

**Q：米家 App 配对失败 / 超时**

- 确保手机在配对过程中始终连接家庭 WiFi（不是引导 WiFi）
- 家庭 WiFi 需为 2.4GHz（Matter over WiFi 不支持纯 5GHz）
- 短按设备按键重启引导 WiFi，重新获取配对码后再试

---

**Q：配对成功但控制没有反应（BLE 无广播）**

短按设备按键手动触发一次广播，确认 BLE 功能正常。若仍无反应，尝试长按出厂重置后重新配对。

---

**Q：想换一台米家账号设备使用**

长按 > 3 秒出厂重置，再重新走配网流程即可。
