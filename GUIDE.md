# 小爱音箱 BLE 唤醒器 — 从烧录到使用

ESP32-C3 通过**涂鸦(Tuya) IoT 平台 MQTT** 接收开关指令，触发 BLE 广播唤醒小爱音箱。
无需 Matter 认证，任何 ESP32-C3 开发板均可使用。

---

## 目录

1. [硬件准备](#1-硬件准备)
2. [开发环境安装](#2-开发环境安装)
3. [获取代码](#3-获取代码)
4. [编译 & 烧录](#4-编译--烧录)
5. [涂鸦平台准备](#5-涂鸦平台准备)
6. [首次配置（WiFi 热点）](#6-首次配置wifi-热点)
7. [服务配置（局域网）](#7-服务配置局域网)
8. [日常使用](#8-日常使用)
9. [按键说明](#9-按键说明)
10. [LED 状态说明](#10-led-状态说明)
11. [修改配置](#11-修改配置)
12. [常见问题](#12-常见问题)

---

## 1. 硬件准备

| 器件 | 说明 |
|---|---|
| ESP32-C3 开发板 | 合宙 AIR32C3 或兼容板均可 |
| USB-C 数据线 | 用于烧录，需支持数据传输 |
| 5V 电源 | 烧录完成后日常供电用 |

**引脚定义（代码默认值）：**

| 功能 | GPIO |
|---|---|
| 触发按键 | GPIO 9（低电平触发，内部上拉）|
| 状态 LED | GPIO 12 |
| 开关状态 LED | GPIO 13 |

---

## 2. 开发环境安装

1. 下载安装 [VS Code](https://code.visualstudio.com/)
2. 打开 VS Code → 扩展（Ctrl+Shift+X）→ 搜索 `PlatformIO IDE` → 安装
3. 等待 PlatformIO 完成初始化（约 2-5 分钟）

> 本版本使用标准 espressif32 平台，首次编译约下载 200MB 工具链，后续编译约 30-60 秒。

---

## 3. 获取代码

```bash
git clone https://github.com/yooooohyou/xiaoai_switch2_wake_up.git
cd xiaoai_switch2_wake_up
code .
```

---

## 4. 编译 & 烧录

### 4.1 连接设备

将 ESP32-C3 通过 USB-C 连接电脑。若设备未识别，安装 [CH343 驱动](https://www.wch.cn/downloads/CH343SER_ZIP.html)。

### 4.2 编译 & 烧录

点击 VS Code 底部工具栏的 **✓ Build** 编译，再点 **→ Upload** 烧录。

> **烧录失败？** 按住 BOOT 键不放 → 按一下 RST → 松开 BOOT，再重试。

烧录成功后设备自动重启，LED 开始快速闪烁。

---

## 5. 涂鸦平台准备

在配置设备之前，需要先在涂鸦 IoT 平台完成以下步骤。

### 5.1 注册并登录

前往 [iot.tuya.com](https://iot.tuya.com/) 注册开发者账号并登录。

### 5.2 创建产品

1. 进入 **产品开发** → 点击 **新建产品**
2. 品类选择：**电工照明 → 开关**（或其他支持开关功能的品类）
3. 连接方式：**Wi-Fi**
4. 记录生成的 **ProductID**

### 5.3 确认 DP 标识符

1. 进入产品的 **功能定义** 页
2. 找到控制开关的功能点，查看其**标识符（Identifier）**
3. 记录该标识符（通常为 `switch_1`）

> 如果功能列表为空，需点击"添加功能" → 选择"开关" → 类型选"布尔型"。

### 5.4 创建测试设备，获取证书

1. 进入 **设备管理** → 点击 **添加设备** → 选择 **手动录入**
2. 填写设备名称，获得设备后点击 **设备详情**
3. 找到 **设备证书** 区域，记录：
   - **Device ID**（设备 ID）
   - **Device Secret**（设备密钥）

---

## 6. 首次配置（WiFi 热点）

烧录完成后，按以下步骤配置 WiFi。

### 步骤一：连接配置热点

设备上电后 LED 快速闪烁，自动开启配置热点：

| 项目 | 内容 |
|---|---|
| WiFi 名称 | `ESP32_BLE_Wake` |
| WiFi 密码 | `12345678` |

用手机或电脑连接该热点。

### 步骤二：填写 WiFi 信息

浏览器访问 `http://192.168.4.1`，填写**家庭 WiFi 名称和密码**（仅支持 2.4GHz），点击 **"保存并连接"**。

### 步骤三：等待连接

设备重启后自动连接家庭 WiFi：

- LED **中速闪烁**（每 0.5 秒）= 正在连接
- LED **慢速闪烁**（每 2 秒）= 连接成功

---

## 7. 服务配置（局域网）

WiFi 连接成功后，配置涂鸦云参数。

### 访问配置页

手机/电脑连接**家庭 WiFi**，浏览器访问：

```
http://esp32c3-wake.local
```

或使用串口监视器查看设备 IP，访问 `http://{设备IP}`。

### 填写涂鸦云参数

| 配置页字段 | 填写内容 | 示例 |
|---|---|---|
| 设备 ID（Device ID）| 涂鸦平台设备证书中的 Device ID | `26c24759c134403843vran` |
| 设备密钥（Device Secret）| 涂鸦平台设备证书中的 Device Secret | `6rtII6krWaGXAUnO` |
| 开关 DP 标识符 | 产品功能定义中的标识符 | `switch_1` |

### 填写 BLE 唤醒目标（可选）

| 配置页字段 | 填写内容 |
|---|---|
| 目标设备 BLE MAC | 目标小爱音箱的 BLE MAC 地址 |
| BLE 广播数据（HEX）| 唤醒数据包（抓包获取） |

> BLE 参数留空将使用代码中的默认值。

点击 **"保存并重启"**，设备重启后自动连接涂鸦云。

---

## 8. 日常使用

设备接入涂鸦云后，可通过以下方式控制：

### 涂鸦智能 App

下载**涂鸦智能**（Tuya Smart）App，用与 iot.tuya.com 相同的账号登录，设备会自动出现在设备列表。

| 操作 | 设备行为 |
|---|---|
| 开关"开" | 发送 BLE 广播（持续约 1 秒），唤醒目标小爱音箱 |
| 开关"关" | 停止 BLE 广播 |

### 语音控制

涂鸦智能 App 支持接入**小爱同学**、**天猫精灵**、**Google Home**、**Amazon Alexa** 等语音助手，在 App 内绑定后可语音控制。

### 自动化

在涂鸦智能 App 中可配置自动化场景，例如每天早上 7 点自动唤醒音箱。

---

## 9. 按键说明

设备上的按键（GPIO 9）：

| 操作 | 配置模式时 | 正常运行时 |
|---|---|---|
| **短按**（< 3 秒）| 重启配置热点 | 手动触发一次 BLE 唤醒广播（测试用）|
| **长按**（> 3 秒）| 出厂重置 | 出厂重置（清除所有配置）|

> **出厂重置**后设备会重启，LED 重新快闪，需重新走配置流程。

---

## 10. LED 状态说明

| LED 状态 | 含义 |
|---|---|
| 快速闪烁（每 0.3 秒）| 配置模式，等待 WiFi 配置 |
| 中速闪烁（每 0.5 秒）| 正在连接 WiFi |
| 慢速闪烁（每 2 秒）| 已连接，正常运行 |
| GPIO 13 常亮 | 当前状态为"开"（收到 on 指令）|
| GPIO 13 熄灭 | 当前状态为"关" |

---

## 11. 修改配置

配置完成后，随时可通过浏览器修改参数：

| 时机 | 访问方式 |
|---|---|
| 正常运行时 | 连接家庭 WiFi → 访问 `http://esp32c3-wake.local` |
| WiFi 无法连接时 | 长按 > 3 秒出厂重置，重新走配置流程 |

修改后点击 **"保存并重启"** 生效。

---

## 12. 常见问题

**Q：192.168.4.1 打不开**

确认手机已连接 `ESP32_BLE_Wake` 热点，而非家庭 WiFi。部分手机会自动切回家庭 WiFi，需在系统设置中手动保持连接。

---

**Q：设备 LED 一直中速闪烁（WiFi 连接失败）**

- 确认 WiFi 名称和密码正确
- 确认家庭 WiFi 为 2.4GHz（不支持纯 5GHz）
- 长按 > 3 秒出厂重置，重新填写 WiFi 信息

---

**Q：esp32c3-wake.local 无法访问**

- 确认手机/电脑与设备在同一局域网
- 部分 Android 设备不支持 mDNS，请改用 IP 地址访问（IP 地址可在路由器管理页或串口输出中查看）

---

**Q：MQTT 连接失败（串口显示 state=-2 或 state=5）**

| 错误码 | 原因 | 解决方法 |
|---|---|---|
| `-2` | 网络不通 | 检查 WiFi 是否连接，涂鸦服务器是否可达 |
| `-4` | 连接超时 | NTP 未同步导致时间戳无效，等待重试 |
| `5` | 认证拒绝 | Device ID 或 Device Secret 填写有误，重新检查 |

---

**Q：收到 MQTT 指令但 BLE 唤醒无效**

短按设备按键手动触发一次广播，确认 BLE 功能正常。若仍无效，检查目标设备 BLE MAC 地址是否正确。

---

**Q：想更换涂鸦账号或设备**

访问 `http://esp32c3-wake.local` 直接修改设备 ID 和密钥，保存重启即可。无需出厂重置。

---

**Q：编译报错 ArduinoJson 相关**

确认 `platformio.ini` 中包含：

```ini
lib_deps =
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^7.0
```

执行 PlatformIO → Clean，再重新编译。
