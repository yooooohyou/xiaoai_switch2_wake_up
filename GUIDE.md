# 小爱音箱 BLE 唤醒器 — 从烧录到使用

ESP32-C3 通过**巴法云 MQTT** 接收开关指令，触发 BLE 广播唤醒小爱音箱。
无需 Matter 认证，任何 ESP32-C3 开发板均可使用。

---

## 目录

1. [硬件准备](#1-硬件准备)
2. [开发环境安装](#2-开发环境安装)
3. [获取代码](#3-获取代码)
4. [编译 & 烧录](#4-编译--烧录)
5. [首次配置（Web 界面）](#5-首次配置web-界面)
6. [接入米家 / 小爱同学](#6-接入米家--小爱同学)
7. [日常使用](#7-日常使用)
8. [按键说明](#8-按键说明)
9. [LED 状态说明](#9-led-状态说明)
10. [修改配置](#10-修改配置)
11. [常见问题](#11-常见问题)

---

## 1. 硬件准备

| 器件 | 说明 |
|---|---|
| ESP32-C3 开发板 | 合宙 AIR32C3 或兼容板均可 |
| USB-C 数据线 | 用于烧录，需支持数据传输 |
| 5V 电源 | 烧录完成后日常供电用 |

**引脚定义（代码默认值）：**

| 功能 | GPIO |
|---|---|
| 触发按键 | GPIO 9（低电平触发，内部上拉）|
| 状态 LED | GPIO 12 |
| 开关状态 LED | GPIO 13 |

---

## 2. 开发环境安装

### 安装 VS Code + PlatformIO

1. 下载安装 [VS Code](https://code.visualstudio.com/)
2. 打开 VS Code → 扩展（Ctrl+Shift+X）→ 搜索 `PlatformIO IDE` → 安装
3. 等待 PlatformIO 完成初始化（约 2-5 分钟）

> **注意：** 本版本使用标准 espressif32 平台（无需 Matter SDK），首次编译时约下载 200MB 工具链，后续编译约 30-60 秒。

---

## 3. 获取代码

```bash
git clone https://github.com/yooooohyou/xiaoai_switch2_wake_up.git
cd xiaoai_switch2_wake_up
```

用 VS Code 打开该目录：

```bash
code .
```

---

## 4. 编译 & 烧录

### 4.1 连接设备

将 ESP32-C3 通过 USB-C 连接电脑。

- **合宙 AIR32C3**：直接插上即可识别，无需额外驱动
- 若设备未识别，安装 [CH343 驱动](https://www.wch.cn/downloads/CH343SER_ZIP.html)

### 4.2 编译

点击 VS Code 底部工具栏的 **✓ Build**，或按快捷键：

```
Ctrl+Alt+B
```

编译成功的标志：

```
========================= [SUCCESS] Took XX.XX seconds =========================
```

### 4.3 烧录

点击底部工具栏的 **→ Upload**，或按快捷键：

```
Ctrl+Alt+U
```

烧录成功后设备自动重启，LED 开始快速闪烁。

> **烧录失败？** 部分 ESP32-C3 需要手动进入 Boot 模式：按住 BOOT 键不放 → 按一下 RST 键 → 松开 BOOT 键，再重试烧录。

---

## 5. 首次配置（Web 界面）

烧录完成后，按以下步骤完成配置：

### 步骤一：连接配置热点

设备上电后 LED 快速闪烁，自动开启配置热点：

| 项目 | 内容 |
|---|---|
| WiFi 名称 | `ESP32_BLE_Wake` |
| WiFi 密码 | `12345678` |

用手机或电脑连接该热点。

### 步骤二：打开配置页面

连接后，浏览器访问：

```
http://192.168.4.1
```

### 步骤三：填写配置参数

页面分三部分：

**WiFi 设置**
- **WiFi 名称**：家庭 WiFi 的 SSID（仅支持 2.4GHz）
- **WiFi 密码**：WiFi 密码

**巴法云 MQTT 设置**（见[第 6 节](#6-接入米家--小爱同学)获取参数）
- **巴法云私钥（UID）**：从 bemfa.com 个人中心复制
- **主题名称**：在巴法云控制台创建的主题名，如 `light001`

**BLE 唤醒目标**（可选，留空使用默认值）
- **目标设备 BLE MAC**：目标小爱音箱的 BLE MAC 地址
- **BLE 广播数据（HEX）**：唤醒数据包，抓包获取

### 步骤四：保存重启

填写完毕后点击 **"保存并重启"**，设备自动重启并连接家庭 WiFi。

- LED 由快闪变为**中速闪烁**（连接中）
- 连接成功后变为**慢闪**（每 2 秒闪一次）

---

## 6. 接入米家 / 小爱同学

### 6.1 注册巴法云账号

1. 访问 [bemfa.com](https://bemfa.com/) 注册账号
2. 登录后进入 **个人中心** → 复制 **私钥（UID）**

### 6.2 创建主题

1. 进入 **控制台** → 点击 **新建主题**
2. 填写主题名（英文，如 `light001`）
3. 类型选择 **灯** 或 **开关**
4. 记下主题名，填入设备配置页

### 6.3 接入米家

1. 下载 **巴法云 App**（或在微信搜索"巴法云"小程序）
2. 登录账号后，在 App 中找到 **米家接入** 选项
3. 按提示在米家 App 中授权巴法云
4. 授权完成后，主题对应的设备会出现在米家设备列表

### 6.4 接入小爱同学

在巴法云 App 内找到 **小爱同学接入**，绑定后即可用语音控制：

```
"小爱同学，打开 light001"
```

---

## 7. 日常使用

配置完成后，通过米家 App 或小爱同学操作：

| 指令 | 设备行为 |
|---|---|
| 米家"开" / 语音"打开" | 发送 BLE 广播（持续约 1 秒），唤醒目标小爱音箱 |
| 米家"关" / 语音"关闭" | 停止 BLE 广播 |

也可以加入**米家自动化**，例如每天早上 7 点自动唤醒音箱。

---

## 8. 按键说明

设备上的按键（GPIO 9）：

| 操作 | 配置模式时 | 正常运行时 |
|---|---|---|
| **短按**（< 3 秒）| 重启配置热点 | 手动触发一次 BLE 唤醒广播（测试用）|
| **长按**（> 3 秒）| 出厂重置 | 出厂重置（清除所有配置）|

> **出厂重置**后设备会重启，LED 重新快闪，需重新走配置流程。

---

## 9. LED 状态说明

| LED 状态 | 含义 |
|---|---|
| 快速闪烁（每 0.3 秒）| 配置模式，等待 Web 配置 |
| 中速闪烁（每 0.5 秒）| 正在连接 WiFi |
| 慢速闪烁（每 2 秒）| 已连接，正常运行 |
| GPIO 13 常亮 | 当前状态为"开"（收到 on 指令）|
| GPIO 13 熄灭 | 当前状态为"关" |

---

## 10. 修改配置

配置完成后，随时可通过浏览器修改参数：

| 时机 | 访问方式 |
|---|---|
| 正常运行时 | 连接家庭 WiFi → 访问 `http://esp32c3-wake.local` 或设备 LAN IP |
| WiFi 无法连接时 | 长按 > 3 秒出厂重置，重新配置 |

修改后点击 **"保存并重启"** 生效。

---

## 11. 常见问题

**Q：192.168.4.1 打不开**

- 确认手机已连接 `ESP32_BLE_Wake`，而不是家庭 WiFi
- 部分手机连接无互联网 WiFi 时会自动切换回家庭 WiFi，需在系统设置中手动保持连接
- 短按设备按键重启配置热点

---

**Q：设备 LED 一直中速闪烁（连接 WiFi 失败）**

- 确认 WiFi 名称和密码填写正确
- 确认家庭 WiFi 为 2.4GHz（ESP32-C3 不支持纯 5GHz）
- 长按 > 3 秒出厂重置，重新填写 WiFi 信息

---

**Q：设备 LED 慢闪但米家无法控制（MQTT 连接失败）**

- 检查串口输出中的 MQTT 连接状态（`state=` 后面的数字）
- 确认巴法云私钥和主题名填写正确
- 登录 bemfa.com 确认主题存在且状态正常
- 设备会每 5 秒自动重试 MQTT 连接

---

**Q：收到 MQTT 指令但 BLE 唤醒无效**

- 短按设备按键手动触发一次广播，确认 BLE 功能正常
- 检查目标 BLE MAC 地址是否正确（格式：`78:81:8c:06:9a:c4`）
- 确认广播数据（HEX）与小爱音箱匹配

---

**Q：想换一个巴法云账号或主题**

长按 > 3 秒出厂重置，或连接家庭 WiFi 访问配置页修改。

---

**Q：编译报错**

确认 `platformio.ini` 中的平台配置正确：

```ini
platform = espressif32
lib_deps = knolleary/PubSubClient@^2.8
```

执行 PlatformIO → Clean，再重新编译。
