# 小爱音箱 BLE 唤醒器 — 从烧录到使用

ESP32-C3 通过 MQTT 接收开关指令，收到"开"时发出一段 BLE 广播唤醒小爱音箱。

**这一版是「统一固件」**：串口只烧一次，之后换云平台、改参数、升级固件全部走网页。

---

## 目录

1. [硬件准备](#1-硬件准备)
2. [开发环境安装](#2-开发环境安装)
3. [获取代码](#3-获取代码)
4. [编译 & 烧录（只需一次）](#4-编译--烧录只需一次)
5. [首次配置（WiFi 热点）](#5-首次配置wifi-热点)
6. [选择云平台并填参数](#6-选择云平台并填参数)
7. [日常使用](#7-日常使用)
8. [网页升级固件（OTA）](#8-网页升级固件ota)
9. [按键与 LED](#9-按键与-led)
10. [诊断页](#10-诊断页)
11. [常见问题](#11-常见问题)

---

## 1. 硬件准备

| 器件 | 说明 |
|---|---|
| ESP32-C3 开发板 | 合宙 AIR32C3 或兼容板均可，需 4MB flash |
| USB-C 数据线 | 用于烧录，需支持数据传输 |
| LED × 2（可选）| 状态指示，不接也能用 |
| 轻触按键（可选）| 多数开发板自带 BOOT 键接在 GPIO9 |

| ESP32-C3 | 连接 |
|---|---|
| GPIO9 | 按钮一端（另一端接地，用内部上拉）|
| GPIO12 | 运行状态 LED 正极（负极接地）|
| GPIO13 | 开关状态 LED 正极（负极接地）|

---

## 2. 开发环境安装

装 [VSCode](https://code.visualstudio.com/)，在扩展市场里搜 **PlatformIO IDE** 安装。
首次启动会自动下载工具链，等它跑完。

---

## 3. 获取代码

```bash
git clone https://github.com/yooooohyou/xiaoai_switch2_wake_up.git
cd xiaoai_switch2_wake_up
git checkout claude/unified-mqtt-ota-YsnYv
```

用 VSCode 打开这个目录，PlatformIO 会自动识别。

---

## 4. 编译 & 烧录（只需一次）

插上开发板，然后：

```bash
pio run -t upload      # 编译并烧录
pio device monitor     # 查看串口输出（115200）
```

烧录不进去时，按住 BOOT 键再插 USB，进下载模式后重试。

> **关于分区表**：这一版用 `min_spiffs.csv`，把 flash 分成两个各约 1.9MB 的 app
> 分区互为备份，这是网页 OTA 能工作的前提。如果编译提示 app 分区放不下，
> 看 `platformio.ini` 里的注释，需要换成 NimBLE 协议栈。

烧完这一次之后，后面所有改动都可以走网页，不用再插线。

---

## 5. 首次配置（WiFi 热点）

设备上电后 LED 快速闪烁，自动开启配置热点：

| 项目 | 内容 |
|---|---|
| WiFi 名称 | `ESP32_BLE_Wake` |
| WiFi 密码 | `12345678` |

手机连上这个热点，浏览器访问 `http://192.168.4.1`，填写**家庭 WiFi 名称和密码**
（仅支持 2.4GHz），点"保存并连接"。

设备重启后自动连家里的 WiFi：

- LED **中速闪烁**（0.5 秒）= 正在连接
- LED **慢速闪烁**（2 秒）= 连接成功

---

## 6. 选择云平台并填参数

手机/电脑连**家庭 WiFi**，浏览器访问：

```
http://esp32c3-wake.local
```

（Android 有些机型不支持 mDNS，改用路由器或串口里看到的 IP 地址。）

页面顶部的「云平台」下拉决定用哪套接入方式，选中哪个就只显示哪个的字段。
**三个平台的参数各自独立保存**，来回切换不会丢。

### 6.1 巴法云（想让小爱直接控制，选这个）

免费、不用审核，而且能通过米家同步给小爱同学。

1. 注册 [cloud.bemfa.com](https://cloud.bemfa.com/)，右上角头像里复制**私钥**
2. 设备管理 → 新建主题。**主题名的后三位是设备类型码**：

   | 后缀 | 类型 |
   |---|---|
   | `001` | 插座 |
   | `002` | 灯 |
   | `003` | 风扇 |
   | `004` | 传感器 |
   | `005` | 空调 |

   建议直接叫 `xiaoai001`。
3. 配置页填私钥和主题名，保存重启
4. 米家 App → 我的 → 其他平台设备 → 添加 → 找到「巴法」→ 绑定巴法云账号 → 同步设备

之后小爱就能语音控制了。

### 6.2 涂鸦 TuyaLink

1. [iot.tuya.com](https://iot.tuya.com/) 建云项目和产品（品类选开关，连接方式 Wi-Fi）
2. 设备管理 → 添加设备 → 设备详情 → **设备证书**，记下 DeviceID 和 DeviceSecret
3. 记下云项目所在的**数据中心**，配置页要选一致的：

   | 数据中心 | 接入地址 |
   |---|---|
   | 中国 | `m1.tuyacn.com` |
   | 美西 | `m1.tuyaus.com` |
   | 欧洲 | `m1.tuyaeu.com` |
   | 印度 | `m1.tuyain.com` |

4. **开关 DP 标识符留空**——新版设备详情页已经不展示它了，固件会从云端下发的
   第一条指令里自动学到，并用同一个标识符回报状态

设备详情页那三个状态的含义：

- **是否激活 = 是** —— 凭证正确，设备成功连过云端
- **当前在线** —— 此刻是否保持着 MQTT 连接（页面是缓存，要点刷新）
- **绑定用户 / 绑定 APP** —— 为空表示还没在涂鸦智能 App 里绑定，App 发不出指令

> 涂鸦设备要进米家，还得再过 Matter 桥或 Home Assistant。目标只是让小爱能喊的话，
> 巴法云那条路短得多。

### 6.3 通用 MQTT

接 Home Assistant、EMQX、自建 broker 或其他标准 MQTT 服务。

| 字段 | 说明 |
|---|---|
| 服务器地址 / 端口 | broker 的地址和端口 |
| 加密方式 | 明文 TCP，或 TLS（不校验证书）|
| ClientID | 留空自动按芯片 ID 生成 |
| 用户名 / 密码 | broker 要求才填 |
| 订阅主题 | 从这个主题收指令 |
| 上报主题 | 状态回报到这里，留空则不回报 |
| JSON 字段路径 | 留空按纯文本匹配；填了则先解析 JSON 再取值 |
| 「开」/「关」对应内容 | 纯文本模式下用来比对的字符串 |

**纯文本模式**（JSON 路径留空）：直接比对整条消息。
除了你填的那两个字符串，`on`/`off`/`1`/`0`/`true`/`false` 也一律识别。

**JSON 模式**：填字段路径，支持用点号进入下一层。比如消息是

```json
{"state": {"power": true}}
```

就填 `state.power`。取到的值可以是布尔、数字，也可以是字符串
（再按「开」/「关」那两栏比对）。

Home Assistant 的一个最小例子：

```yaml
mqtt:
  switch:
    - name: "小爱唤醒"
      command_topic: "home/xiaoai/set"
      state_topic: "home/xiaoai/state"
      payload_on: "on"
      payload_off: "off"
```

配置页对应填：订阅主题 `home/xiaoai/set`，上报主题 `home/xiaoai/state`，
JSON 路径留空，开/关分别 `on` / `off`。

---

## 7. 日常使用

App 或语音说"打开" → 云端下发 → 设备发 1 秒 BLE 广播唤醒小爱音箱。
说"关闭"则停止广播。

想脱离云端单独验证 BLE 那半边，访问 `http://esp32c3-wake.local/wake`，
或者短按一下设备按键。

---

## 8. 网页升级固件（OTA）

改完代码后：

```bash
pio run                # 只编译，不烧录
```

产物在 `.pio/build/airm2m_core_esp32c3/firmware.bin`。

浏览器访问 `http://esp32c3-wake.local/update`，选这个 `.bin` 上传，
写完自动重启，大约十几秒后就是新固件了。

几点说明：

- 升级过程中保持供电和网络。**中途断电不会变砖**——新固件写的是备份分区，
  没写完不会切过去，重启还是旧固件
- 固件文件第一个字节必须是 `0xE9`，传错文件会被挡下来并提示
- 升级不会清空配置，WiFi 和平台参数都还在
- 上传期间设备会主动断开 MQTT，升级完自己连回来

---

## 9. 按键与 LED

| 操作 | 配置模式 | 正常模式 |
|---|---|---|
| 短按 | 重启配置热点 | 手动触发一次 BLE 唤醒广播 |
| 长按 > 3 秒 | 出厂重置，清除所有配置 | 同左 |

| LED 闪烁 | 含义 |
|---|---|
| 0.3 秒（快）| 配置热点模式 |
| 0.5 秒（中）| 正在连接 WiFi |
| 2 秒（慢）| 已连接 |

GPIO13 那颗 LED 跟随开关状态：收到"开"常亮，"关"熄灭。

---

## 10. 诊断页

`http://esp32c3-wake.local/status`，每 5 秒自动刷新。出问题先看这一页：

| 项目 | 怎么用 |
|---|---|
| 固件版本 | 确认 OTA 有没有生效 |
| 平台 | 当前用的哪套接入方式，参数完整没 |
| WiFi | 连的哪个网、IP、信号强度（RSSI 低于 -75 dBm 就偏弱了）|
| NTP 时间 | 没同步的话涂鸦签名做不出来，会直接跳过连接 |
| MQTT / 最近返回码 | 连没连上，没连上是什么原因（中文提示）|
| 连接成功 / 失败次数 | 两个数都在涨 = 在反复重连，看堆内存和信号 |
| DP 标识符 | 涂鸦模式下，有没有学到标识符 |
| 最近一条指令 | 云端到底有没有把指令发下来 |
| 剩余堆内存 | BLE 和 TLS 共存时比较紧张，这里看余量 |

---

## 11. 常见问题

**Q：192.168.4.1 打不开**

确认手机连的是 `ESP32_BLE_Wake` 热点而不是家庭 WiFi。部分手机发现热点没有外网会
自动切回去，需要在系统设置里手动保持连接。

---

**Q：LED 一直中速闪烁（连不上 WiFi）**

- 确认 WiFi 名称和密码正确
- 确认是 2.4GHz（不支持纯 5GHz）
- 长按 > 3 秒出厂重置，重新填

---

**Q：esp32c3-wake.local 打不开**

确认手机和设备在同一个局域网。部分 Android 不支持 mDNS，改用 IP 地址访问，
IP 可以在路由器管理页或串口输出里看到。

---

**Q：MQTT 连不上**

看诊断页的「最近返回码」：

| 错误码 | 原因 | 怎么办 |
|---|---|---|
| `-4` | 服务器无响应 | 地址或端口不通；涂鸦模式确认数据中心选对 |
| `-3` | 连接被断开 | 网络抖动或内存不足，看剩余堆内存和 RSSI |
| `-2` | TCP/TLS 建连失败 | 检查 WiFi 能不能出网，确认服务器地址 |
| `4` | 用户名或密码错误 | 涂鸦是 Secret 填错或设备时间偏差过大；通用模式检查账号密码 |
| `5` | 认证未通过 | 涂鸦：DeviceID / Secret / 数据中心有一项不匹配 |

---

**Q：涂鸦平台一直显示离线**

先看设备详情里的**是否激活**：

- **是** —— 凭证没问题，设备连过云端。离线说明此刻没保持连接：没通电、没连上
  WiFi，或者连上又掉了。看诊断页的「连接成功 / 失败次数」，两个数都在涨就是在
  反复重连，重点查堆内存和 WiFi 信号
- **否** —— 从来没连上过，按上一条的返回码排查

平台上的在线状态是缓存快照，要点**刷新**才更新。

---

**Q：设备在线，但 App 里找不到它**

涂鸦模式下看设备详情的**绑定用户 / 绑定 APP**，为空说明还没有 App 账号绑定这台
设备。在涂鸦智能 App 里添加设备完成绑定后才能下发指令。

---

**Q：收到指令但 BLE 唤醒无效**

访问 `/wake` 或短按按键手动触发一次，确认 BLE 本身是通的。还不行就检查配置页里
目标设备的 BLE MAC 和广播数据是否正确。

---

**Q：OTA 上传到一半失败**

- 「这不是 ESP32 固件文件」= 传错文件了，要传 `.pio/build/…/firmware.bin`，
  不是 `firmware.elf` 也不是 `bootloader.bin`
- 空间不足 = 固件超出 1.9MB 分区，按 `platformio.ini` 注释换 NimBLE
- 中途断网 = 重连后再传一次，旧固件没被破坏

---

**Q：编译提示 app 分区放不下**

这一版为了支持 OTA 用了双分区表，单个分区约 1.9MB。Bluedroid 蓝牙协议栈比较占
地方，超了的话换 NimBLE 能省 300~500KB，具体见 `platformio.ini` 里的注释。

实在不想动，也可以把 `board_build.partitions` 改回 `huge_app.csv`——代价是
失去网页 OTA，以后升级还得插线。
